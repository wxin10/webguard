# WebGuard frontend production build template.

VITE_API_BASE_URL=https://api.webguard.example.com

# Keep false for production. When true, the Web app mirrors access tokens to
# localStorage for local extension manual-token fallback only.
VITE_ENABLE_DEV_TOKEN_STORAGE=false
